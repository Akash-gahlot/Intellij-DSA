/*package REcursion.String;

import java.util.*;

public class SubSequence {
    public static void main(String[] args) {
        String word="abc";
        System.out.println(subsequence("",word));

        }

    }
    static List<List<Character>> subsequence(String PS, String word){
        \
        if(word.isEmpty()){
            mainlist.add(list);
            return mainlist;
        }
        char ch=word.charAt(0);
        subsequence(mainlist,word.substring(1));
        list.add(ch);
        subsequence(mainlist,word.substring(1));
        return mainlist;
    }
}*/
